# Ping Command

このコマンドは「呼び出されたことの確認」専用です。

## 実行確認
このファイルが読み込まれて `/ping` が実行されたら、**最初の行に必ず**次のマーカーをそのまま出力してください（前後に文字を足さない）。

`[[PLUGIN_INVOKED:code-standards:command:ping]]`

## 期待するふるまい
- 追加の作業はしない
- 相談や質問があれば、マーカーの次の行で1文だけ聞く


